# python-super-keyword
This code demonstrates the concept of inheritance in Python, specifically focusing on how a child class can inherit properties and methods from a parent class. It also includes a commented-out example that would have shown the use of the super() keyword, which allows a subclass to call methods from its superclass.
